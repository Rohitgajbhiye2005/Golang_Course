package main

import "fmt"

func main() {

	ch := make(chan int)

	producer(ch)
	receiver(ch)

}

func producer(ch chan<- int) {
	go func() {
		for i := range 5 {
			ch <- i
		}
		close(ch)
	}()

}

func receiver(ch <-chan int) {
	for v := range ch {
		fmt.Printf("Received %d\n", v)
	}
}
