package main

import (
	"fmt"
	"os"
)

func main() {


	//os doenst even let the defer to run its immidiate exit 

	defer fmt.Println("deffered function")

	fmt.Println("starting function")
	// exit with status code 1
	// staus code 0 is for succesed and other are for errors
	os.Exit(1)

	fmt.Println("ending statement")

}
